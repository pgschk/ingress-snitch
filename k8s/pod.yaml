apiVersion: v1
kind: Pod
metadata:
  name: snitch
spec:
  containers:
  - name: snitch
    image: quay.io/pgschk/ingress-snitch:latest
    env:
    - name: TRAEFIK_API_URL
      value: "http://traefik.k8s0.i.blafaselblub.net:9000/api"